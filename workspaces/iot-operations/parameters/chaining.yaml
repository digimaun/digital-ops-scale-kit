# Output chaining for aio-instance step
# Contains outputs from steps that run BEFORE aio-instance

# From schema-registry step
schemaRegistryId: "{{ steps.schema-registry.outputs.schemaRegistry.id }}"

# From adr-ns step
adrNamespaceId: "{{ steps.adr-ns.outputs.adrNamespace.id }}"

# From aio-enablement step
clExtensionIds: "{{ steps.aio-enablement.outputs.clExtensionIds }}"
