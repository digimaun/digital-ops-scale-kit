# Example release preview

This committed example exercises candidate preparation, attestation, installation,
and the release preview. It is accepted only in a dry run and cannot publish a
release or create a tag.
